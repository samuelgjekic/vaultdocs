# VaultDocs — frontend

React 18 + Vite + TypeScript + Tiptap. The SPA half of the VaultDocs monorepo.

See the [top-level README](../README.md) for the full quickstart, tech stack, and architecture notes.

## Local dev

```sh
npm install --legacy-peer-deps
cp .env.example .env
npm run dev
```
